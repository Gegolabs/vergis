node_modules
**/node_modules
dist
*.log.jsonl
asistencia.html
hello.html
out
.git
.DS_Store
# Secretos: NUNCA al build context (el stage de build hace COPY . .).
.env
.env.*
*.env
# Contexto innecesario para el runtime (achica la imagen y la superficie).
docs
tests
.github
local
*.md
# …salvo el CHANGELOG, que SÍ va a la imagen: es lo único que contesta «¿qué exige esta versión?»
# desde la VM, sin salir de ella (issue #229). La excepción va después del patrón que la excluiría.
!CHANGELOG.md
